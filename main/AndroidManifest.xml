<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sean.itr1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SearchPage" />
        <activity android:name=".AddNewSeries" />
        <activity android:name=".NoShowExistsWindow" />
        <activity android:name=".MyArchivePage" />
        <activity android:name=".AppSettings" />
        <activity android:name=".MyProfile" />
        <activity android:name=".ListDisplay" />
    </application>

</manifest>
